function h(e) {
  var g = [];
  if (e.length === 0)
    return "";
  if (typeof e[0] != "string")
    throw new TypeError("Url must be a string. Received " + e[0]);
  if (e[0].match(/^[^/:]+:\/*$/) && e.length > 1) {
    var t = e.shift();
    e[0] = t + e[0];
  }
  e[0].match(/^file:\/\/\//) ? e[0] = e[0].replace(/^([^/:]+):\/*/, "$1:///") : e[0] = e[0].replace(/^([^/:]+):\/*/, "$1://");
  for (var i = 0; i < e.length; i++) {
    var l = e[i];
    if (typeof l != "string")
      throw new TypeError("Url must be a string. Received " + l);
    l !== "" && (i > 0 && (l = l.replace(/^[\/]+/, "")), i < e.length - 1 ? l = l.replace(/[\/]+$/, "") : l = l.replace(/[\/]+$/, "/"), g.push(l));
  }
  var f = g.join("/");
  f = f.replace(/\/(\?|&|#[^!])/g, "$1");
  var u = f.split("?");
  return f = u.shift() + (u.length > 0 ? "?" : "") + u.join("&"), f;
}
function a() {
  var e;
  return typeof arguments[0] == "object" ? e = arguments[0] : e = [].slice.call(arguments), h(e);
}
export {
  a as default
};
