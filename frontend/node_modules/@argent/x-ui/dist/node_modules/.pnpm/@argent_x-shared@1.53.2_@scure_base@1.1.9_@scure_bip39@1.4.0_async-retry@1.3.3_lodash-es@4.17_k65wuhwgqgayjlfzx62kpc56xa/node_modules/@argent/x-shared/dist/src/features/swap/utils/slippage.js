import { bipsToPercent as i } from "../../../utils/bips.js";
import { TradeType as a } from "../models/trade.model.js";
import { add as s, div as n } from "../../../bigdecimal/lib.js";
import { ONE as c } from "../../../bigdecimal/constants.js";
function f(e, t) {
  const o = i(t), r = {
    value: BigInt(e.receiveAmount),
    decimals: e.receiveToken.decimals
  };
  if (e.tradeType === a.EXACT_RECEIVE)
    return r;
  const m = s(c, o);
  return n(r, m);
}
export {
  f as minimumAmountOutFromTrade
};
