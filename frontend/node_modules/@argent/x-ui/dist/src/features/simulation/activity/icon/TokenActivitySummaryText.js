import { prettifyTokenAmount as m, ensureDecimals as r } from "@argent/x-shared";
import { useToken as i } from "../../../../providers/TokenProvider.js";
const d = ({ summary: o, networkId: n }) => {
  const e = o.asset.tokenAddress, t = i({
    networkId: n,
    address: e
  });
  if (!t)
    return "Unknown token";
  const s = o.asset.amount || 0;
  return m({
    amount: s,
    decimals: r(t.decimals),
    symbol: t.symbol
  });
};
export {
  d as TokenActivitySummaryText
};
