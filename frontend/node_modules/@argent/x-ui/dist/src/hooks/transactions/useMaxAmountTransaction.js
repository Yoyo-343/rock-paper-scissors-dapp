import { getUint256CalldataFromBN as o } from "@argent/x-shared";
import { useMemo as r } from "react";
import { CallData as m } from "starknet";
const u = (a, t) => r(() => a && t ? [
  {
    contractAddress: a.address,
    entrypoint: "transfer",
    calldata: m.compile({
      recipient: t,
      amount: o(a.balance)
    })
  }
] : [], [a, t]);
export {
  u as useMaxAmountTransaction
};
